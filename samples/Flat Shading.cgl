shader main {
    input vec3 position;
    input vec3 normal;
    output vec4 fragColor;

    void main() {
        vec3 color = vec3(0.5, 0.5, 0.5) + 0.5 * normal;
        fragColor = vec4(color, 1.0);
    }
}
